// Tailwind CSS v4 is configured via CSS @theme variables and the vite plugin.
// This file is present to satisfy project structure requirements.
export default {
    // Content paths are automatically detected in v4, but can be specified here if needed.
}
